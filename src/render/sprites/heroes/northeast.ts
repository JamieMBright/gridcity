// North-East England's bespoke-hero registry. Filled by Wave W5 (subagent) from
// docs/heroes/northeast/ — each a hand-drawn iso sprite with its own bespoke
// night electrification light, matched to a placed name (Tyne Bridge, Angel of
// the North, Durham Cathedral…). Empty ⇒ this fabric bakes no heroes and
// renders byte-identically.
import type { BespokeHero } from './registry';

export const CITY_HEROES: BespokeHero[] = [];
